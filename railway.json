{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "(cd gio_v3 && npm ci && npm run build:css) || echo 'Aviso: no se pudo recompilar Tailwind, sirviendo gio_v3/static/css/tailwind-built.css ya versionado en el repo'; pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "gunicorn \"app:create_app()\" --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --log-level debug --capture-output",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
